# Divide-and-Conquer-Algorithm

This code is To divide the input list of teams into subproblems, each subproblem consisting of exactly 2 teams (a match) that will then be processed in later stages (the conquer step)
